def capitalizeWords(arr):
    result = []
    if len(arr) == 0:
        return result
    result.append(arr[0].title())
    return result + capitalizeWords(arr[1:])
capitalizeWords(['abc','def','ghi'])
